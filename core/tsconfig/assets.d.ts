// Maintenance note: Make sure this file always represents
// the same asset types as the ones supported by core/build.

// The reason we have this file in core/tsconfig and not core/build
// is so that react library packages only have to depend on core/tsconfig to get valid types.

import "vite/client";
