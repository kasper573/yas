import { atoms } from "@yas/style";

export const relativeFill = atoms({
  width: "100%",
  height: "100%",
  position: "relative",
});

export const dock = atoms({
  position: "absolute",
  inset: 0,
});
