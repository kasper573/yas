import { style } from "@yas/style";

export const relativeFill = style({
  width: "100%",
  height: "100%",
  position: "relative",
});

export const dock = style({
  position: "absolute",
  inset: 0,
});
