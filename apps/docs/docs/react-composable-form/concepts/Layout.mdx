---
hide_table_of_contents: true
---

import * as component from "./Layout.components";
import * as code from "!!slice-loader!./Layout.components";
import {Split} from "../../../src/components/Split";
import CodeBlock from "@theme/CodeBlock";

Layout is a fundamental concept of this library. They allow you to reuse an existing form and
recompose its fields using a layout of your choice.

## Card layout

<Split names={["Code", "Form"]}>
  <CodeBlock language="tsx">{code.LoginForm}</CodeBlock>
  <component.LoginForm />
</Split>

## Inline layout

<Split names={["Code", "Form"]}>
  <CodeBlock language="tsx">{code.InlineLoginForm}</CodeBlock>
  <component.InlineLoginForm />
</Split>

## Specialized layout

<Split names={["Code", "Form"]}>
  <CodeBlock language="tsx">{code.SpecializedLoginForm}</CodeBlock>
  <component.SpecializedLoginForm />
</Split>
